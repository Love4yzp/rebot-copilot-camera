# runtime data — teach/playback routines live here, never committed
# (root-anchored: an unanchored "routines/" also swallows backend/routines/)
/routines/
# v2 libraries — same operator-data treatment as /routines/
/poses/
/sequences/
/templates/
# drop-in plugins — the user's own code, synced by device.sh push, never committed
/plugins/

# python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# node / frontend
node_modules/
frontend/dist/
backend/static/

# platformio (firmware/esp32-shutter)
.pio/
.vscode/

# os
.DS_Store

# typescript incremental build cache
*.tsbuildinfo

# AI
.kimi/
.agent_context/