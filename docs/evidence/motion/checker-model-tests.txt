Validation evidence captured after production worker compilation was restored:

```text
cd app && .venv/bin/pytest -q tests/test_physics_playback.py -k checker
......                                                                   [100%]
6 passed, 2 deselected in 0.18s

cd app && .venv/bin/pytest -q tests/test_physics_model.py
.............                                                            [100%]
13 passed in 7.35s
```

The checker includes negative cases for empty/missing physical runs, missing
scenario results, zero samples, WAIT command gaps, and drifting hold targets.

Exit codes: `CHECKER_RC=0`, `MODEL_RC=0`.
